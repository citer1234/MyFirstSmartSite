/* Общий стиль */
body {
    font-family: Arial, sans-serif;
    background: #667eea;
    margin: 0;
    padding: 20px;
    color: #333;
}

/* Контейнер */
body > * {
    background: white;
    padding: 20px;
    border-radius: 10px;
    max-width: 600px;
    margin: auto;
}

/* Заголовок */
h1 {
    text-align: center;
}

/* Поле ввода */
textarea {
    width: 100%;
    padding: 10px;
    border-radius: 6px;
    border: 1px solid #ccc;
}

/* Кнопка */
button {
    margin-top: 10px;
    padding: 10px 20px;
    border: none;
    background: #ff758c;
    color: white;
    border-radius: 20px;
    cursor: pointer;
}

/* Наведение на кнопку */
button:hover {
    background: #ff5a73;
}

/* Блок результата */
.result {
    margin-top: 20px;
    padding: 15px;
    background: #f1f1f1;
    border-radius: 8px;
}